<Window x:Class="hciProjekat.IzborViseSoftvera"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hciProjekat"
        mc:Ignorable="d"
        Title="IzborViseSoftvera" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" Grid.Row="0" x:Name="dgrSoftveri" 
                  Margin="0,55,0,0" Height="155" Width="440" MaxWidth="560"
                  AutoGenerateColumns="False"
                  HorizontalAlignment="Left" VerticalAlignment="Top"
                  ItemsSource="{Binding Path=Softveri, Mode=TwoWay}" Foreground="#FF196C70">
            <DataGrid.BorderBrush>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="#FF36BDC3" Offset="1"/>
                </LinearGradientBrush>
            </DataGrid.BorderBrush>
            <DataGrid.Columns>
                <DataGridTextColumn 
                    Header="Oznaka"
                    Width="Auto"
                    IsReadOnly="True"
                    Binding="{Binding Softver.Id}" />
                <DataGridTextColumn
                        Width="Auto"
                        Binding="{Binding Softver.Naziv}"
                        IsReadOnly="True"
                        Header="Naziv" />
                <DataGridCheckBoxColumn
                    Width="Auto"
                    Header="Odabran"
                    IsReadOnly="False"
                    Binding="{Binding Odabran}"
                    />
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Column="1" Grid.Row="1" x:Name="Potvrdi" Height="35" Width="75" Content="Potvrdi" Click="Potvrdi_Click"></Button>

    </Grid>
</Window>
